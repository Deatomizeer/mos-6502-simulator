using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// This class holds a string-to-object map that allows the 
public class OpcodeHolder : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

}
